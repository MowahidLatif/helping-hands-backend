# Stripe (donation payments)
# Temporary test keys from Stripe Dashboard -> Developers -> API keys
# When STRIPE_SECRET_KEY is empty, backend returns fake clientSecret for local testing
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_CURRENCY=usd

# S3 / MinIO (media uploads)
# Use MinIO locally or set AWS credentials for production.
# S3_* vars can be set later when configuring AWS.
S3_ENDPOINT=http://127.0.0.1:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=media-dev
S3_USE_PATH_STYLE=true
